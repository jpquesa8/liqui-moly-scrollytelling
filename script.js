// Detectamos el movimiento del usuario al hacer scroll

window.addEventListener("scroll", () => {

    // Posición actual del scroll
    const scrollPosition = window.scrollY;

    console.log("Scroll:", scrollPosition);

});

